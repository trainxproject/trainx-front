import Calendario from "@/components/Calendar"

const ClasesView = () => {
    return(
        <div className="w-4xl h-10 bg-(--secondary)">
            <h2 className="text-(--foreground) text-3xl font-bold">Clendario</h2>    
        </div>
    )
}

export default ClasesView
